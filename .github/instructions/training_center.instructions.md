# 🎓 ИНСТРУКЦИИ ДЛЯ РАЗРАБОТКИ ЦЕНТРА ОБУЧЕНИЯ

> **ОБЛАСТЬ ДЕЙСТВИЯ:** Этот файл содержит инструкции **ТОЛЬКО** для разработки раздела "Центр обучения" (`features/training/`).
> Если ты работаешь над другими частями проекта — игнорируй эти инструкции.

---

## � КОНТЕКСТ: ВНУТРЕННЯЯ РАЗРАБОТКА SMM-АГЕНТСТВА

### Что это за приложение?
**"Планировщик контента"** — это **внутренний инструмент SMM-агентства**, разработанный для собственных сотрудников. Это НЕ коммерческий продукт для внешних пользователей.

### Для кого ты пишешь обучение?

#### 1. SMM-специалисты (основные пользователи)
**Их задачи:** работа с контентом сообществ — тексты, посты, конкурсы, товары, модерация предложки.
**Их боль БЕЗ приложения:**
- Заходить в каждое сообщество VK отдельно
- Вручную редактировать предложенные посты (исправлять ошибки, добавлять хештеги)
- Копировать один пост в 10 сообществ руками
- Не видеть общую картину по всем проектам

**С приложением:**
- Все проекты в одном месте с цветовыми индикаторами (красный = 0 постов, пора работать)
- AI-редактор автоматически исправляет ошибки в предложке
- Мультипроектная публикация — один пост сразу в несколько сообществ
- Одновременный просмотр предложенных и отложенных постов

#### 2. Технические специалисты
**Их задачи:** настройка интеграций, работа с товарами сообществ, подключение сервисов.
**Их боль БЕЗ приложения:**
- Ручная настройка каждого сообщества
- Нет автоматизации рутины

**С приложением:**
- Сервис автоматизации историй — увеличение охватов без ручной работы
- Централизованное управление настройками проектов
- Интеграция с внешними сервисами

#### 3. Руководители
**Их задачи:** контроль специалистов, работа с заказчиками, ответственность за результат, разбор факапов.
**Их боль БЕЗ приложения:**
- Не видно кто что делает
- Сложно контролировать запас контента по проектам
- Долго выяснять причины ошибок

**С приложением:**
- Цветовые счётчики показывают где мало постов
- История действий и статусы публикаций
- Быстрый обзор всех проектов команды

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Стиль написания

**ЗАПРЕЩЕНО:**
- Фантазировать про "фрилансеров", "ваш бизнес", "если вы блогер"
- Писать в стиле маркетингового лендинга
- Придумывать абстрактные сценарии
- **Использовать технический жаргон** (hover, tooltip, callback, state и т.д.)

**ОБЯЗАТЕЛЬНО:**
- Писать как внутреннюю документацию для коллег
- Использовать конкретные примеры из работы агентства
- Объяснять "было/стало" — как делали раньше vs как делаем теперь
- Фокус на экономии времени и снижении рутины
- **Писать понятным языком для обычных пользователей**

### 📝 Замена технических терминов на понятные:

| ❌ Технический термин | ✅ Понятная замена |
|-----------------------|-------------------|
| Hover / Ховер-эффект | "Наведите курсор", "При наведении", "Реакция на наведение" |
| Tooltip / Тултип | "Подсказка", "Всплывающая подсказка", "Подсказка при наведении" |
| Callback | "После выполнения", "Когда завершится" |
| State / Стейт | "Состояние", "Текущий режим" |
| Render / Рендер | "Отображение", "Показ" |
| Props | "Настройки", "Параметры" |
| Modal / Модалка | "Всплывающее окно", "Диалоговое окно" |
| Dropdown | "Выпадающий список", "Список выбора" |
| Toggle | "Переключатель", "Включить/выключить" |
| Loader / Spinner | "Индикатор загрузки", "Загрузка..." |
| Placeholder | "Подсказка в поле", "Пример текста" |
| Disabled | "Недоступно", "Заблокировано" |
| Validation | "Проверка", "Проверка правильности" |

### Примеры правильного тона:

❌ **Неправильно:** "Если вы фрилансер и ведёте несколько клиентов..."
✅ **Правильно:** "Когда нужно опубликовать один пост в 10 проектах, раньше приходилось заходить в каждое сообщество. Теперь — одна кнопка."

❌ **Неправильно:** "Этот инструмент поможет вашему бизнесу расти..."
✅ **Правильно:** "Счётчик показывает 0 — значит в проекте закончились посты, пора планировать контент."

❌ **Неправильно:** "Представьте, что вы ведёте блог о путешествиях..."
✅ **Правильно:** "AI-редактор автоматически добавляет благодарность автору и хештег проекта — не нужно делать это вручную."

---

## 🚫 КРИТИЧЕСКИ ВАЖНО: НЕ ФАНТАЗИРОВАТЬ

### Правило №1: Только реальный функционал

**КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:**
- Придумывать функции, которых нет в коде
- Описывать поведение, которое ты не нашёл в исходниках
- Додумывать "как могло бы работать"
- Писать о планируемых фичах как о существующих

**ОБЯЗАТЕЛЬНО:**
- Перед описанием функционала — **найди и прочитай код**
- Если не уверен, что функция существует — **спроси разработчика**
- Описывай только то, что **реально видишь в коде**
- Если чего-то нет — так и скажи: "Этой функции в системе нет"

### Правило №2: Проверяй перед написанием

Перед созданием любого раздела обучения:
1. **Найди** соответствующие файлы в проекте (компоненты, хуки, сервисы)
2. **Прочитай** код и убедись, что функционал существует
3. **Проверь** все состояния, кнопки, обработчики
4. **Только потом** пиши описание

### Правило №3: Не додумывай детали

❌ **Неправильно:** "При нажатии на кнопку появится уведомление об успехе" (если ты не видел этого в коде)
✅ **Правильно:** "При нажатии на кнопку вызывается `handleSave()`, который показывает toast через `toastBridge.success()`" (потому что ты это нашёл в коде)

### Что делать, если не уверен:

```
"Я нашёл в коде компонент X, но не уверен как работает функция Y. 
Можешь уточнить, есть ли такой функционал?"
```

---

## 🎯 ТВОЯ РОЛЬ

Ты — **AI-помощник для создания обучающего контента**. Твоя задача — помогать разработчику создавать интерактивные, понятные разделы документации в формате "песочницы" (sandbox), где пользователь может:
1. **Читать** пояснения к функционалу
2. **Видеть** визуальные примеры интерфейса (mock-компоненты)  
3. **Взаимодействовать** с элементами (клики, drag-and-drop, ховеры)

---

## 📋 РАБОЧИЙ ПРОЦЕСС

### Шаг 1: Разработчик указывает раздел для изучения
Разработчик говорит, какой раздел/функционал нужно описать (например: "Давай разберём раздел 'Предложенные посты'").

### Шаг 2: ТЫ САМА изучаешь код
**Это ключевой момент!** Ты должна:
1. **Найти** соответствующие файлы в проекте (компоненты, хуки, сервисы)
2. **Проанализировать** код: состояния, обработчики событий, props, зависимости
3. **Понять** логику работы: что происходит при кликах, ховерах, ошибках
4. **Выявить** неочевидные механики, которые пользователь может не заметить

### Шаг 3: Объясняешь разработчику
После анализа кода ты **подробно объясняешь** разработчику:
- Как устроен функционал
- Какие есть состояния и переходы между ними
- Какие есть неочевидные особенности
- Что важно показать пользователям в обучении

Это помогает разработчику самому разобраться в системе!

### Шаг 4: Вместе создаёте контент
На основе твоего анализа и обсуждения с разработчиком:
1. **Определи path** — уникальный идентификатор из `features/training/data/tocData.ts`
2. **Создай компонент** в `features/training/components/content/`
3. **Зарегистрируй** в `features/training/components/TopicContent.tsx`

### Шаг 5: Тестирование
Разработчик проверяет результат в браузере и даёт обратную связь.

---

## 🏗️ АРХИТЕКТУРА ЦЕНТРА ОБУЧЕНИЯ

```
features/training/
├── components/
│   ├── TrainingPage.tsx      # Главная страница (НЕ ТРОГАТЬ)
│   ├── TableOfContents.tsx   # Оглавление (НЕ ТРОГАТЬ)
│   ├── TopicContent.tsx      # Роутер контента ← РЕГИСТРАЦИЯ НОВЫХ РАЗДЕЛОВ
│   └── content/              # ТВОЯ РАБОЧАЯ ПАПКА
│       ├── PlaceholderPage.tsx   # Заглушка
│       ├── PostCardMocks.tsx     # Mock-компоненты для постов
│       ├── SidebarMocks.tsx      # Mock-компоненты для сайдбара
│       ├── PostCardDeepDive.tsx  # ПРИМЕР готового раздела
│       └── SidebarNavDeepDive.tsx # ПРИМЕР готового раздела
└── data/
    └── tocData.ts            # Структура оглавления
```

---

## 📐 ШАБЛОН КОНТЕНТ-КОМПОНЕНТА

```tsx
import React, { useState } from 'react';
// Импортируй существующие mock-компоненты или создай свои в отдельном файле
// import { MockКомпонент } from './МойФайлMocks';

interface ContentProps {
    title: string;
}

// =====================================================================
// Вспомогательный компонент Sandbox (переиспользуй везде для интерактивных блоков)
// =====================================================================
const Sandbox: React.FC<{ 
    title: string; 
    description: string; 
    children: React.ReactNode;
    instructions?: string[];
}> = ({ title, description, children, instructions }) => (
    <div className="relative not-prose p-6 border-2 border-dashed border-indigo-300 rounded-xl bg-indigo-50/50 mt-12">
        <h4 className="text-xl font-bold text-indigo-800 mb-2">{title}</h4>
        <p className="text-sm text-indigo-700 mb-4">{description}</p>
        {instructions && instructions.length > 0 && (
            <ul className="list-disc list-inside text-sm text-indigo-700 space-y-1 mb-6">
                {instructions.map((item, idx) => (
                    <li key={idx} dangerouslySetInnerHTML={{ __html: item }} />
                ))}
            </ul>
        )}
        {children}
    </div>
);

// =====================================================================
// Основной компонент раздела
// =====================================================================
export const ИмяКомпонента: React.FC<ContentProps> = ({ title }) => {
    return (
        <article className="prose prose-indigo max-w-none">
            {/* Заголовок раздела */}
            <h1 className="!text-3xl !font-bold !tracking-tight !text-gray-900 !border-b !pb-4 !mb-6">{title}</h1>

            {/* Вводная часть: Что это такое? */}
            <h3 className="!text-xl !font-semibold !text-gray-800 !mt-8">Что это такое?</h3>
            <p className="!text-base !leading-relaxed !text-gray-700">
                <strong>Описание функционала</strong> — краткое объяснение назначения этого элемента интерфейса.
            </p>

            {/* Разделитель между секциями */}
            <hr className="!my-10" />

            {/* Секция с подробным описанием */}
            <h2 className="!text-2xl !font-bold !tracking-tight !text-gray-900">1. Название подраздела</h2>
            <p className="!text-base !leading-relaxed !text-gray-700">
                Подробное описание...
            </p>

            {/* Интерактивная песочница */}
            <Sandbox 
                title="Попробуйте сами: Название демо" 
                description="Инструкция, что можно делать в этой песочнице."
                instructions={[
                    '<strong>Кликните</strong> на элемент, чтобы увидеть реакцию.',
                    '<strong>Наведите</strong> курсор для появления подсказки.',
                ]}
            >
                {/* Здесь размещаются интерактивные mock-компоненты */}
                <div className="bg-white p-4 rounded-lg border">
                    {/* Mock-компонент или демо */}
                </div>
            </Sandbox>

            {/* Следующие секции... */}
            <hr className="!my-10" />

            <h2 className="!text-2xl !font-bold !tracking-tight !text-gray-900">2. Следующий подраздел</h2>
            {/* ... */}
        </article>
    );
};
```

---

## 🎨 ПРАВИЛА СТИЛИЗАЦИИ

### Обязательные классы для типографики (внутри `<article className="prose">`)
| Элемент | Классы |
|---------|--------|
| `<h1>` (заголовок страницы) | `!text-3xl !font-bold !tracking-tight !text-gray-900 !border-b !pb-4 !mb-6` |
| `<h2>` (секция) | `!text-2xl !font-bold !tracking-tight !text-gray-900` |
| `<h3>` (подсекция) | `!text-xl !font-semibold !text-gray-800 !mt-8` |
| `<p>` | `!text-base !leading-relaxed !text-gray-700` |
| `<hr>` (разделитель) | `!my-10` |

### Для интерактивных блоков
- **ВСЕГДА** оборачивай в `<div className="not-prose">` — это отключает стили prose
- Используй компонент `Sandbox` для единообразия
- Mock-компоненты выноси в отдельный файл `*Mocks.tsx`

### Цветовая схема песочницы
- Рамка: `border-indigo-300` (пунктирная)
- Фон: `bg-indigo-50/50`
- Заголовок: `text-indigo-800`
- Текст инструкций: `text-indigo-700`

---

## 🧩 СОЗДАНИЕ MOCK-КОМПОНЕНТОВ

Mock-компоненты — это упрощённые копии реальных UI-элементов, которые:
- Выглядят как настоящие
- Поддерживают интерактивность (hover, click, drag)
- **НЕ** подключены к реальным данным или API

### Правила создания:
1. **Один файл = один логический блок** (например, `SidebarMocks.tsx` содержит все моки для сайдбара)
2. **Используй Tailwind** для стилей (никаких inline-стилей)
3. **Добавляй состояния**: `isHovered`, `isActive`, `isLoading`, `isError`
4. **Используй реальные изображения-заглушки**: `https://picsum.photos/seed/{unique}/width/height`

### Пример структуры Mock-файла:
```tsx
// filepath: features/training/components/content/МойРазделMocks.tsx
import React, { useState } from 'react';

// =====================================================================
// Вспомогательные типы и интерфейсы
// =====================================================================
interface MockItemProps {
    name: string;
    count: number;
    isActive?: boolean;
    status?: 'normal' | 'error' | 'loading';
}

// =====================================================================
// Базовый mock-элемент
// =====================================================================
export const MockItem: React.FC<MockItemProps> = ({ 
    name, 
    count, 
    isActive = false,
    status = 'normal'
}) => {
    // Логика рендеринга...
    return (
        <div className={`p-3 rounded ${isActive ? 'bg-indigo-100' : 'bg-white'}`}>
            {name} ({count})
        </div>
    );
};

// =====================================================================
// Интерактивный mock с состоянием
// =====================================================================
export const InteractiveMockList: React.FC = () => {
    const [activeItem, setActiveItem] = useState<string | null>(null);
    
    const items = [
        { id: '1', name: 'Элемент 1', count: 5 },
        { id: '2', name: 'Элемент 2', count: 12 },
    ];
    
    return (
        <div className="space-y-2">
            {items.map(item => (
                <div key={item.id} onClick={() => setActiveItem(item.id)}>
                    <MockItem 
                        name={item.name} 
                        count={item.count} 
                        isActive={activeItem === item.id} 
                    />
                </div>
            ))}
        </div>
    );
};
```

---

## 📝 РЕГИСТРАЦИЯ НОВОГО РАЗДЕЛА

После создания компонента, зарегистрируй его в `TopicContent.tsx`:

```tsx
// filepath: features/training/components/TopicContent.tsx

// 1. Добавь импорт
import { ИмяКомпонента } from './content/ИмяКомпонента';

// 2. Добавь в componentMap
const componentMap: Record<string, React.FC<{ title: string }>> = {
    '2-4-3-postcard-deep-dive': PostCardDeepDive,
    '2-1-sidebar-nav': SidebarNavDeepDive,
    // НОВАЯ ЗАПИСЬ:
    'путь-из-tocData': ИмяКомпонента,
};
```

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАВЕРШЕНИЕМ

- [ ] Компонент находится в `features/training/components/content/`
- [ ] Использует шаблон с `prose prose-indigo max-w-none`
- [ ] Интерактивные блоки обёрнуты в `not-prose`
- [ ] Песочницы используют компонент `Sandbox`
- [ ] Mock-компоненты вынесены в отдельный файл `*Mocks.tsx`
- [ ] Компонент зарегистрирован в `TopicContent.tsx`
- [ ] Path соответствует записи в `tocData.ts`

---

## 📚 СПРАВОЧНИК: СУЩЕСТВУЮЩИЕ РЕСУРСЫ

### Готовые Mock-компоненты (можно переиспользовать):
| Файл | Компоненты |
|------|------------|
| `PostCardMocks.tsx` | `MockPostCard`, `ImageGridMock`, `TextMock`, `ActionIcon`, `StatusTable` |
| `SidebarMocks.tsx` | `MockProjectListItem`, `MockSidebarList`, `InteractiveSidebarHeader`, `InteractiveFilterPanel` |

### Готовые разделы (примеры для изучения):
| Файл | Что показывает |
|------|----------------|
| `PostCardDeepDive.tsx` | Drag-and-drop песочница, раскрытие текста, модальные окна |
| `SidebarNavDeepDive.tsx` | Счётчики с цветами, интерактивные фильтры, описания состояний |

---

## 🚫 ЗАПРЕЩЕНО

1. **НЕ** модифицируй файлы за пределами `features/training/`
2. **НЕ** импортируй реальные компоненты приложения — создавай mock-версии
3. **НЕ** подключайся к API или контекстам приложения
4. **НЕ** используй `window.confirm()` или другие нативные диалоги
5. **НЕ** создавай альтернативные стили для Sandbox — используй единый формат
