
# 9. Руководство для нового разработчика (Onboarding)

Добро пожаловать в команду разработки **VK Content Planner**! Это руководство поможет вам быстро погрузиться в архитектуру проекта и начать писать код, соответствующий нашим стандартам.

## 1. Ментальная модель приложения

Прежде чем смотреть код, поймите, как "думает" это приложение:

1.  **Бэкенд — это мозг.** Фронтенд старается быть "глупым". Мы не дублируем бизнес-логику валидации (например, проверку конфликтов времени) на клиенте. Мы отправляем данные и ждем ответа (или ошибки) от сервера.
2.  **ProjectsContext — это сердце.** Если вам нужны данные (посты, проекты, настройки), они там. Не делайте локальных `fetch` запросов внутри компонентов.
3.  **FSD (Feature-Sliced Design) Lite.** Мы группируем код по *смыслу* (фичам), а не по типу файлов. Если вы делаете фичу "Товары", весь код (компоненты, хуки, типы) должен лежать в `features/products`.

## 2. Ваш первый таск: Алгоритм действий

Допустим, вам нужно добавить новую кнопку "Экспорт в Excel" в сайдбар проектов.

### Шаг 1: Бэкенд (API)
1.  Создайте схему запроса/ответа в `backend_python/schemas/`.
2.  Напишите логику в `backend_python/services/`.
3.  Создайте эндпоинт в `backend_python/routers/`.
4.  Проверьте через Swagger UI (`http://127.0.0.1:8000/docs`).

### Шаг 2: Слой сервисов (Frontend)
1.  Откройте `src/services/api/project.api.ts` (или создайте новый файл, если фича крупная).
2.  Добавьте функцию `exportToExcel`, использующую `callApi`.
3.  Экспортируйте её через `src/services/api.ts`.

### Шаг 3: Логика (Hooks)
1.  Найдите хук, управляющий сайдбаром (`features/projects/hooks/...` или контекст).
2.  Добавьте туда функцию-обработчик, которая вызовет API и обработает результат (например, скачает файл).

### Шаг 4: UI (Components)
1.  Добавьте кнопку в `Sidebar.tsx`.
2.  Свяжите её с функцией из хука.

## 3. Частые ошибки и "Грабли"

### 🔴 "Моргание" изображений
*   **Причина:** Определение компонента внутри другого компонента.
*   **Решение:** Никогда не пишите `const Child = () => { ... }` внутри `const Parent = () => { ... }`. Вынесите `Child` наружу или в отдельный файл.

### 🔴 Z-Index Hell
*   **Причина:** Модальное окно перекрывается другим или выпадающее меню обрезается `overflow: hidden`.
*   **Решение:** Используйте **React Portals**. Все модальные окна и всплывающие меню (`ActionsDropdown`, `ImagePreviewModal`) должны рендериться в `document.body`.

### 🔴 Stale Closures (Устаревшие замыкания)
*   **Причина:** Использование значений из стейта внутри `useEffect` или `useCallback` без добавления их в массив зависимостей.
*   **Решение:** Всегда следите за линтером (ESLint). Если он просит добавить зависимость — добавьте, либо используйте `useRef` для значений, которые не должны вызывать ре-рендер.

### 🔴 Проброс пропсов (Prop Drilling)
*   **Причина:** Передача данных через 5 уровней компонентов.
*   **Решение:** Используйте композицию хуков. Если данные нужны глубоко, возможно, стоит вынести их в контекст или использовать "умный" компонент-контейнер, который подключится к хуку напрямую.

## 4. Полезные команды

*   `npm run dev` — Запуск фронтенда.
*   `npm run build` — Сборка для продакшена.
*   `uvicorn main:app --reload` — Запуск бэкенда (из папки `backend_python`).
