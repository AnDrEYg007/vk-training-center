# Сайдбар и Навигация по проектам

**Компонент:** `features/projects/components/Sidebar.tsx`
**Элемент списка:** `features/projects/components/ProjectListItem.tsx`

## 1. Логика фильтрации и сортировки

Сайдбар отвечает за отображение списка проектов с учетом различных фильтров. Вся логика фильтрации реализована внутри компонента `Sidebar`.

### Фильтры
1.  **Поиск:** Фильтрация по названию проекта (`searchQuery`).
2.  **Команда:** Фильтрация по полю `team` (`teamFilter`). Список команд формируется динамически (`uniqueTeams`) на основе данных всех проектов.
3.  **Контент (Content Filter):** Специфичный фильтр, зависящий от текущей вкладки (`activeView`).
    *   Если активны "Отложенные": фильтрует по `scheduledPostCounts`.
    *   Если активны "Предложенные": фильтрует по `suggestedPostCounts`.
    *   *Варианты:* "Нет постов" (0), "Есть посты" (>0), "< 5", "5-10", "> 10".

### Разделение списков
Список проектов разделен на две части:
1.  **Активные:** Отображаются сразу.
2.  **Отключенные (`disabled: true`):** Скрыты в спойлере внизу списка, чтобы не загромождать интерфейс.

## 2. Интерактивность `ProjectListItem`

Каждый элемент списка — это сложный интерактивный компонент.

### Анимация (Slide-in panel)
При наведении курсора на проект (`onMouseEnter`) слева выезжает панель с кнопками действий ("Обновить", "Настройки").
*   **Реализация:** Используется `transform: translateX` для панели и `padding-left` для текста названия.
*   **Стабилизация:** Кнопки внутри панели позиционированы абсолютно, чтобы избежать "дрожания" во время CSS-транзишена.

### Индикаторы состояния
1.  **Счетчик постов:** Цвет фона зависит от количества (красный -> оранжевый -> зеленый -> синий).
2.  **Индикатор обновления (🔵):** Если ID проекта находится в `updatedProjectIds` (из контекста), отображается пульсирующая точка.
3.  **Индикатор ошибки (⚠️):** Если для проекта есть запись в `projectPermissionErrors`, отображается предупреждение.
4.  **Лоадер:** Заменяет счетчик, если для этого проекта идет процесс обновления (`isCheckingForUpdates` или `isSequentiallyUpdating`).

## 3. Массовое обновление

В шапке сайдбара есть кнопка "Массовое обновление" (иконка списка).
*   **Логика:** Она запускает последовательное обновление (`onRefreshProject`) для **всех проектов, видимых в данный момент** (с учетом текущих фильтров).
*   Это позволяет менеджеру отфильтровать проекты, например, "Команда А", и обновить только их одной кнопкой.