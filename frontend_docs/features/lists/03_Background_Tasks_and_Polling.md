
# Фоновые задачи и Polling (System Lists)

**Хук:** `features/lists/hooks/useListPolling.ts`

## 1. Task Monitor Pattern

Все длительные операции (синхронизация) выполняются в фоне. Фронтенд запускает задачу, получает `taskId` и опрашивает статус.

## 2. Специфичные сценарии запуска

### Сбор активностей (`InteractionSyncModal`)
**Компонент:** `InteractionSyncModal.tsx`

При нажатии "Обновить" в списках "Лайки/Комменты/Репосты" открывается модальное окно.
*   **Функционал:** Выбор периода (С...По) или пресета (Неделя, Месяц, Год).
*   **Особенности:** Ограничение в 92 дня снято. Можно собирать данные за любой период.
*   **Результат:** Запускает задачу `refreshInteractionsStream`.

### Анализ диалогов (Mailing Analysis)
В списке "В рассылке" кнопка "Анализ" открывает меню выбора режима:
*   **Дополнить (Missing):** Анализирует только диалоги, где еще не вычислена дата первого сообщения. (Быстро)
*   **Полный пересчет (Full):** Пересчитывает всех. (Медленно)
*   **Результат:** Запускает задачу `analyzeMailingStream`, которая заполняет поля `first_message_date` и `first_message_from_id`.

### Обновление деталей (Subscribers / Authors)
Кнопка `🔄` (в панели фильтров) запускает задачи `refreshSubscriberDetails` или `refreshAuthorDetails`.
*   **Цель:** Обновить аватарки, статусы (бана) и время последнего захода, не меняя состав списка.

### Очистка списка
Доступна только администраторам. Полностью удаляет данные списка из БД через `api.clearListData`.
