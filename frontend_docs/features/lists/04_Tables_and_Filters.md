
# Таблицы и Фильтрация (System Lists)

**Компоненты:** `MembersTable.tsx`, `PostsTable.tsx`, `InteractionTable.tsx`
**Секция:** `features/lists/components/sections/ListsFilters.tsx`
**Логика:** `features/lists/hooks/useListFetching.ts`

## 1. Табличное представление

В зависимости от выбранного списка (`activeList`), рендерится один из трех компонентов таблицы.

### `MembersTable` (Подписчики, История, Рассылка, Авторы)
Универсальная таблица для отображения пользователей.
*   **Базовые данные:** ФИО, ссылка на профиль, аватар, ID.
*   **Статусы и Детали:** Пол, Город, Статус (Активен/Забанен), Онлайн (Last Seen), Платформа (iOS/Android/Web).
*   **Контекстные колонки:**
    *   **Для Рассылки (Mailing):**
        *   **ЛС:** Статус доступности сообщений (✅/🚫).
        *   **FC (First Contact):** Дата первого сообщения в диалоге (результат анализа).
        *   **Init:** Инициатор диалога (👤 Пользователь / 🏢 Сообщество).
        *   **LC (Last Contact):** Дата последнего сообщения (сортировка по умолчанию).
    *   **Для Авторов:**
        *   **Дата события:** Дата, когда пользователь был впервые обнаружен системой как автор поста.
    *   **Для Истории:**
        *   **Дата события:** Дата вступления или выхода из группы.

### `InteractionTable` (Активности)
*   **Раскрытие (Expand):** При клике на строку пользователя таблица раскрывается (аккордеон), показывая список ID постов, с которыми он взаимодействовал. Ссылки на посты генерируются автоматически.

### `PostsTable` (Посты)
*   **Медиа:** Показывает миниатюру первого вложения. Клик открывает `ImagePreviewModal`.
*   **Статистика:** Колонки с лайками, комментами, репостами и просмотрами.

## 2. Система фильтрации

Над таблицей располагается панель `ListsFilters`.

### Доступные фильтры
*   **Поиск:** Полнотекстовый поиск по имени, ID, ссылке (для юзеров) или тексту (для постов).
*   **Качество:** `Все`, `Активные`, `Забаненные`, `Удаленные`.
*   **Демография:** `Пол`, `Возраст` (диапазоны), `Месяц рождения` (для именинников).
*   **Технические:** `Онлайн`, `Платформа` (устройство входа).
*   **Сообщения (Mailing):** `Разрешено` / `Запрещено`.

### Элементы управления в панели фильтров

1.  **Кнопка "Анализ" (Mailing Analysis):**
    *   Доступна только в списке "В рассылке".
    *   Реализована как кнопка с выпадающим меню (через React Portal).
    *   **Опции:**
        *   **Дополнить (Missing):** Быстрый режим. Запускает задачу анализа только для записей, где поля `first_message_date` пусты.
        *   **Полный пересчет (Full):** Медленный режим. Пересчитывает историю диалогов для всех пользователей списка заново.
    *   **Индикация:** Кнопка отображает спиннер и прогресс ("Анализ 50/200") во время выполнения задачи.

2.  **Кнопка "Обновить детали" (🔄):**
    *   Доступна для списков людей (Подписчики, Авторы, История).
    *   Запускает фоновую задачу (`refreshSubscriberDetails` и аналоги), которая обновляет профильные данные (аватар, статус, онлайн) без изменения состава списка.

3.  **Кнопка "Очистить базу" (Admin):**
    *   Позволяет полностью удалить данные текущего списка из БД. Защищена модальным окном подтверждения.

### Реализация фильтрации
Фильтрация происходит **на стороне бэкенда** (SQL `WHERE`). При изменении фильтра сбрасывается пагинация, и отправляется новый запрос к API.

---

## 3. UX и Скролл (Sticky Headers)

*   **Глобальная прокрутка:** Контент находится в едином скролл-контейнере.
*   **Sticky Filters:** Панель фильтров прилипает к верху экрана (`sticky top-0 z-20`) при прокрутке, оставаясь доступной.
*   **Sticky Table Headers:** Заголовки таблиц также закреплены (`sticky top-0 z-10`), но находятся "слоем ниже" панели фильтров, что создает эффект прохождения таблицы под панелью управления.
