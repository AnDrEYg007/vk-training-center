# 🎓 Руководство разработчика: Центр обучения

Добро пожаловать! Этот документ поможет тебе начать работу над Центром обучения — интерактивной документацией для пользователей приложения "Планировщик контента".

---

## ⚠️ ВАЖНО: Инструкции по написанию контента

**Перед началом работы ОБЯЗАТЕЛЬНО прочитай:**  
📖 [**Инструкции для центра обучения**](../../.github/instructions/training_center.instructions.md)

Этот файл содержит:
- ✅ **Правила написания контента** (НЕ фантазировать! Только реальный код!)
- ✅ **Стиль и тон документации** (внутренняя для коллег, БЕЗ технического жаргона)
- ✅ **Замена технических терминов** на понятные (hover → "наведите курсор")
- ✅ **Шаблоны компонентов и Sandbox** (единый стандарт)
- ✅ **Рабочий процесс** AI + разработчик

**🤖 AI-помощник автоматически использует эту инструкцию при работе с проектом.**

---

## 🚀 Быстрый старт

### 1. Открой проект в VS Code с подключённым Claude

### 2. Скажи Claude примерно следующее:

> "Давай начнём работу над центром обучения. Я хочу создать раздел про [название функционала]. Вот что он делает: [описание]. Покажи мне сначала структуру файла tocData.ts, чтобы понять какой path использовать."

### 3. Опиши функционал своими словами

Claude задаст уточняющие вопросы и поможет создать:
- Контент-компонент с пояснениями
- Mock-компоненты для визуализации
- Интерактивные песочницы

---

## 📂 Структура папок

```
features/training/
├── DEVELOPER_GUIDE.md        ← ТЫ ЗДЕСЬ
├── components/
│   ├── TrainingPage.tsx      # Главная страница (не трогай)
│   ├── TableOfContents.tsx   # Оглавление (не трогай)
│   ├── TopicContent.tsx      # ⭐ Сюда регистрируешь новые разделы
│   └── content/              # ⭐ Твоя рабочая папка
│       ├── PlaceholderPage.tsx   # Заглушка для незаполненных разделов
│       ├── PostCardMocks.tsx     # Готовые mock-компоненты
│       ├── SidebarMocks.tsx      # Готовые mock-компоненты
│       ├── PostCardDeepDive.tsx  # ✅ Пример готового раздела
│       └── SidebarNavDeepDive.tsx # ✅ Пример готового раздела
└── data/
    └── tocData.ts            # Структура оглавления (path → title)
```

---

## 🎯 Твои задачи

### Что нужно сделать:
1. **Создавать контент-компоненты** — файлы в `components/content/` с описанием функционала
2. **Создавать mock-компоненты** — визуальные копии UI для демонстрации
3. **Регистрировать разделы** — добавлять записи в `TopicContent.tsx`

### Чего делать НЕ нужно:
- Менять структуру проекта
- Редактировать файлы вне `features/training/`
- Подключаться к реальным API или данным

---

## 📋 Пошаговый процесс создания раздела

### Шаг 1: Выбери раздел из PROGRESS.md

Открой `PROGRESS.md` и выбери раздел со статусом ⬜ (не начато).

### Шаг 2: Попроси Claude изучить код

Скажи Claude:
```
Давай разберём раздел "[Название]" (path: [path]).
Найди в коде проекта как это работает и объясни мне.
```

Claude сам:
- Найдёт нужные файлы (компоненты, хуки, сервисы)
- Проанализирует логику работы
- Объяснит тебе как всё устроено
- Выделит неочевидные моменты

> 💡 Ты учишься параллельно с созданием документации!

### Шаг 3: Обсудите что важно показать пользователям

После того как ты понял функционал, обсудите с Claude:
- Какие состояния показать в песочнице
- Какие "неочевидные" вещи важны для пользователя
- Какие интеракции сделать (клики, drag-and-drop)

### Шаг 4: Claude создаёт компонент

### Шаг 4: Claude создаёт компонент

Claude создаст файл в `components/content/`, например `WelcomeScreen.tsx`:

```tsx
import React from 'react';

interface ContentProps {
    title: string;
}

export const WelcomeScreen: React.FC<ContentProps> = ({ title }) => {
    return (
        <article className="prose prose-indigo max-w-none">
            <h1 className="!text-3xl !font-bold !tracking-tight !text-gray-900 !border-b !pb-4 !mb-6">
                {title}
            </h1>
            
            <h3 className="!text-xl !font-semibold !text-gray-800 !mt-8">Что это такое?</h3>
            <p className="!text-base !leading-relaxed !text-gray-700">
                <strong>Экран приветствия</strong> — это первое, что вы видите при запуске приложения...
            </p>
            
            {/* Добавь остальной контент */}
        </article>
    );
};
```

### Шаг 5: Зарегистрируй компонент

Claude сам добавит в `components/TopicContent.tsx`:

```tsx
// Добавь импорт вверху файла
import { WelcomeScreen } from './content/WelcomeScreen';

// Добавь в componentMap
const componentMap: Record<string, React.FC<{ title: string }>> = {
    '2-4-3-postcard-deep-dive': PostCardDeepDive,
    '2-1-sidebar-nav': SidebarNavDeepDive,
    '1-3-welcome-screen': WelcomeScreen, // ← Новая запись
};
```

### Шаг 6: Проверь результат

Открой приложение в браузере, перейди в Центр обучения и кликни на свой раздел.
Если что-то не так — скажи Claude и он исправит.

---

## 🎨 Формат "Песочница" (Sandbox)

Главная фишка Центра обучения — интерактивные демо. Вот как их делать:

### Компонент Sandbox
```tsx
const Sandbox: React.FC<{ 
    title: string; 
    description: string; 
    children: React.ReactNode;
    instructions?: string[];
}> = ({ title, description, children, instructions }) => (
    <div className="relative not-prose p-6 border-2 border-dashed border-indigo-300 rounded-xl bg-indigo-50/50 mt-12">
        <h4 className="text-xl font-bold text-indigo-800 mb-2">{title}</h4>
        <p className="text-sm text-indigo-700 mb-4">{description}</p>
        {instructions && (
            <ul className="list-disc list-inside text-sm text-indigo-700 space-y-1 mb-6">
                {instructions.map((item, idx) => (
                    <li key={idx} dangerouslySetInnerHTML={{ __html: item }} />
                ))}
            </ul>
        )}
        {children}
    </div>
);
```

### Пример использования
```tsx
<Sandbox 
    title="Попробуйте сами: Переключение вкладок" 
    description="Кликайте по вкладкам, чтобы увидеть переключение."
    instructions={[
        '<strong>Нажмите</strong> на вкладку "Отложенные"',
        '<strong>Обратите внимание</strong> на изменение подсветки',
    ]}
>
    <InteractiveTabsMock />
</Sandbox>
```

---

## 🧱 Mock-компоненты

Mock-компоненты — это упрощённые копии реального UI. Они:
- Выглядят как настоящие элементы
- Поддерживают интерактивность (hover, click)
- НЕ подключены к данным

### Создание нового mock-файла

Если тебе нужны новые mock-компоненты, создай файл `НазваниеMocks.tsx`:

```tsx
// filepath: components/content/TabsMocks.tsx
import React, { useState } from 'react';

export const MockTab: React.FC<{ 
    label: string; 
    isActive: boolean; 
    onClick: () => void;
}> = ({ label, isActive, onClick }) => (
    <button 
        onClick={onClick}
        className={`py-2 px-4 text-sm font-medium border-b-2 ${
            isActive 
                ? 'border-indigo-600 text-indigo-600' 
                : 'border-transparent text-gray-500 hover:text-gray-700'
        }`}
    >
        {label}
    </button>
);

export const InteractiveTabsMock: React.FC = () => {
    const [activeTab, setActiveTab] = useState('scheduled');
    
    return (
        <div className="flex gap-4 border-b border-gray-200 bg-white p-4 rounded-lg">
            <MockTab 
                label="Отложенные" 
                isActive={activeTab === 'scheduled'} 
                onClick={() => setActiveTab('scheduled')} 
            />
            <MockTab 
                label="Предложенные" 
                isActive={activeTab === 'suggested'} 
                onClick={() => setActiveTab('suggested')} 
            />
            <MockTab 
                label="Опубликованные" 
                isActive={activeTab === 'published'} 
                onClick={() => setActiveTab('published')} 
            />
        </div>
    );
};
```

---

## 💬 Как общаться с Claude

### Начало работы над разделом:
> "Давай разберём раздел 'Предложенные посты' (path: 3-1-suggested-posts). Найди в коде как это работает и объясни мне."

### После объяснения:
> "Ок, теперь я понял. Давай сделаем обучающий материал. Обязательно покажи [такую-то фичу] в песочнице."

### Запрос на создание демо:
> "Теперь давай сделаем интерактивную песочницу, где пользователь сможет [действие]."

### Доработка:
> "Добавь в песочницу ещё один вариант состояния: [описание]."

### Финализация:
> "Отлично, теперь покажи финальный код и инструкцию по регистрации в TopicContent.tsx."

---

## ✅ Чеклист готовности раздела

- [ ] Файл создан в `components/content/`
- [ ] Использует `prose prose-indigo max-w-none` для типографики
- [ ] Есть вводный блок "Что это такое?"
- [ ] Интерактивные блоки используют компонент Sandbox
- [ ] Mock-компоненты вынесены в отдельный файл (если нужны новые)
- [ ] Компонент зарегистрирован в `TopicContent.tsx`
- [ ] Раздел отображается корректно в браузере

---

## 📚 Полезные ссылки

| Файл | Зачем смотреть |
|------|----------------|
| `tocData.ts` | Найти path для нужного раздела |
| `PostCardDeepDive.tsx` | Пример сложной песочницы с drag-and-drop |
| `SidebarNavDeepDive.tsx` | Пример описания состояний с цветовыми индикаторами |
| `PostCardMocks.tsx` | Примеры mock-компонентов |
| `TRAINING_STRUCTURE.md` (корень проекта) | Полная структура всех разделов с описанием контента |

---

## ❓ Частые вопросы

### Как узнать, какие разделы уже сделаны?
Посмотри `componentMap` в `TopicContent.tsx` — там перечислены все готовые разделы.

### Что если нужного path нет в tocData.ts?
Сообщи основному разработчику — он добавит новый пункт в оглавление.

### Можно ли использовать реальные компоненты приложения?
Нет. Создавай mock-версии. Это изолирует Центр обучения от изменений в основном коде.

### Как добавить изображение-заглушку?
Используй `https://picsum.photos/seed/{уникальный-ключ}/{ширина}/{высота}`:
```tsx
<img src="https://picsum.photos/seed/demo-1/400/225" alt="Демо" />
```
