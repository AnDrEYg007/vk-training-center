## Компонент: Карточки

Карточки являются основными контейнерами для отображения контента, таких как посты, заметки и предложенные публикации.

### 1. Общая структура

-   **Контейнер**: Белый фон, скругленные углы (`rounded-lg`), легкая тень (`shadow-sm`) и граница (`border`).
-   **Заголовок**: Содержит мета-информацию (время, дата, автор).
-   **Тело**: Основной контент (текст, изображения).
-   **Действия**: Кнопки управления (редактировать, удалить и т.д.), которые **видны всегда** для быстрого доступа.

### 2. Типы карточек

#### a. PostCard (Карточка поста в расписании)

-   **Структура**:
    -   Время публикации.
    -   Сетка изображений (если есть).
    -   Список вложений.
    -   **Блок тегов**: Отображает все присвоенные посту теги в виде цветных плашек.
    -   Кнопки действий.
-   **Состояния**:
    -   `Default`: Стандартный вид.
    -   `Hover`: Усиливается тень (`hover:shadow-md`), делая карточку более заметной.
    -   `Selected`: Появляется синяя рамка (`ring-2 ring-indigo-500`).
    -   `Published`: Карточка слегка затуманена градиентом, слева появляется зеленая иконка "галочка".
    -   `Expanded`: Текст поста полностью развернут.
-   **Вариант: Системный пост (`SystemPost`)**:
    -   **Стиль**: Имеет пунктирную рамку (`border-dashed`) для визуального отличия.
    -   **Индикаторы статуса**: В левом верхнем углу отображается иконка, соответствующая текущему статусу поста:
        | Статус | Иконка | Цвет рамки | Описание |
        | :--- | :---: | :--- | :--- |
        | `pending_publication` | 🕒 | Серая | Ожидает публикации. |
        | `publishing` | ⚙️ (аним.) | Синяя | Публикуется или проверяется. |
        | `possible_error` | ⚠️ | Желтая | Возможная ошибка (не найден на стене). |
        | `error` | ❌ | Красная | Ошибка при публикации. |
    -   **Действия**: Набор кнопок может меняться в зависимости от статуса (например, блокировка кнопок при `publishing`).
    -   **Предпросмотр глобальных переменных**: Текст поста в карточке системного поста отображается с уже **подставленными значениями** глобальных переменных. Это позволяет вам видеть финальный вид текста еще до публикации.

#### b. NoteCard (Карточка заметки)

-   **Структура**:
    -   Цветной фон, зависящий от выбранного цвета заметки.
    -   Время, заголовок (опционально), текст.
-   **Состояния**:
    -   `Collapsed` (свернута): Отображается только время и заголовок.
    -   `Expanded` (развернута): Отображается полный текст и все кнопки действий.
    -   `Selected`: Появляется синяя рамка.

#### c. SuggestedPostCard (Карточка предложенного поста)

-   **Структура**:
    -   Горизонтальная лента изображений (если есть).
    -   Название группы-автора и дата.
    -   Текст поста.
    -   Футер с ссылкой на пост в VK и кнопкой "Редактор AI".
-   **Состояния**:
    -   `Default`: Стандартный вид.
    -   `Hover`: Усиливается тень.
    -   `Selected`: Карточка получает синюю рамку (`border-indigo-500 ring-2`) и остается выделенной. При этом кнопка может менять текст на "Сгенерировать новый".

---

### 3. Доступность (Accessibility)

-   **Обязательные `title`**: Все кнопки-иконки внутри карточек (`PostCard`, `NoteCard`) должны иметь атрибут `title` с описанием действия (например, `title="Редактировать"`). Это помогает пользователям понять назначение кнопки без видимой текстовой подписи.

### 4. Производительность

-   **Ленивая загрузка изображений (Lazy Loading)**: Все изображения внутри карточек должны загружаться "лениво" для ускорения первоначальной отрисовки страницы.
    -   **Реализация**: Используется компонент `LazyImage`, который отображает скелетон-заглушку, пока оригинальное изображение загружается в фоне.

-   **Мемоизация и стабильная композиция**: Для предотвращения ненужных перерисовок и визуальных артефактов (таких как "моргание" изображений) необходимо следовать двум ключевым принципам.
    -   **Проблема**: В сложных интерфейсах, таких как сетка календаря, любое изменение состояния в родительском компоненте (например, переключение видимости тегов) может вызвать каскадную перерисовку всех дочерних элементов. Если дочерние компоненты не оптимизированы, это приводит к потере их внутреннего состояния (например, состояния анимации в `LazyImage`) и, как следствие, к "морганию" уже загруженных картинок.
    -   **Решение**:
        1.  **Мемоизация (`React.memo`)**: Все компоненты-карточки (`PostCard`, `NoteCard`) должны быть обернуты в `React.memo`. Это предотвращает их перерисовку, если их `props` не изменились. Для корректной работы этого подхода необходимо также обеспечить стабильность всех передаваемых `props` (объектов и функций) с помощью хуков `useMemo` и `useCallback` в родительских компонентах.
        2.  **Стабильная композиция (Component Composition)**: **Категорически запрещено** определять дочерние компоненты внутри функции рендеринга родительского компонента. Это является анти-паттерном, так как на каждой перерисовке родителя создается *новый тип* дочернего компонента, что заставляет React **полностью перемонтировать** его, а не обновлять.
            -   **Правильно (как в `PostCard.tsx`)**: Все дочерние компоненты (`ImageGrid`, `AttachmentsDisplay`, `PostTags`) должны быть определены **за пределами** основного компонента `PostCard`. Это гарантирует, что их "личность" стабильна, и React сможет их просто обновить, сохранив их внутреннее состояние.