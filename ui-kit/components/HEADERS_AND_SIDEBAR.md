## Компоненты: Заголовки и Сайдбар

Эти компоненты формируют основной каркас приложения и обеспечивают глобальную навигацию и управление. В приложении существует две основных навигационных панели.

---

### 1. Primary Sidebar (Главная навигационная панель)

Это самая левая панель с иконками, которая видна всегда. Она служит для переключения между основными модулями приложения и доступа к глобальным настройкам.

-   **Структура и Анимация**:
    -   Компонент имеет двухколоночную структуру, реализованную с помощью `display: flex`.
    -   **Первая колонка (основная)**: Фиксированная ширина (`w-16`), содержит вертикальный список иконок-кнопок.
    -   **Вторая колонка (выезжающая)**: Содержит подменю для активного модуля. Ее ширина анимируется с помощью `max-width` от `0` до `max-w-xs`. Это позволяет панели плавно раскрываться на ширину своего контента.
-   **Иконки-модули (Верхняя группа)**:
    -   `Контент-менеджмент`: Активирует модуль `km`, открывая подменю ("Отложенные" / "Предложенные") и сайдбар проектов.
    -   `Работа с сообщениями`: Заглушка для будущего модуля.
    -   `Статистика`: Заглушка для будущего модуля.
-   **Глобальные действия (Нижняя группа)**:
    -   `Управление пользователями` (только для администраторов).
    -   `Управление базой проектов`.
    -   `Центр обучения`.
    -   `Выйти`: Завершает сессию пользователя.

### 2. Sidebar (Сайдбар проектов)

Этот сайдбар появляется слева от основной рабочей области, когда активен модуль "Контент-менеджмент".

-   **Структура**:
    -   **Заголовок**: "Проекты" с кнопками:
        -   **Массовое обновление (иконка "список")**: Запускает последовательное обновление всех **видимых** в данный момент проектов.
        -   **Обновить список проектов (иконка 🔄)**: Принудительно перезагружает список проектов из базы данных.
    -   **Переключатель вида**: Сегментированный переключатель "Отложенные" / "Предложенные", который определяет, какие посты и счетчики будут отображаться.
    -   **Панель фильтров**:
        -   Поиск по названию.
        -   Фильтр по командам (кнопки-теги).
        -   Фильтр по количеству постов (кнопки-теги).
    -   **Список проектов**:
        -   Основной скроллящийся элемент.
        -   Каждый проект — это кликабельная строка (`ProjectListItem`), отображающая название и счетчик постов.
        -   Разделен на активные и отключенные проекты.
-   **Интерактивность `ProjectListItem`**:
    -   **Hover**: Слева "выезжает" панель с кнопками "Обновить" и "Настройки". Анимация реализована через `transform: translateX`, а внутренние элементы стабилизированы через `position: absolute` для предотвращения "прыжков".
    -   **Активный проект**: Подсвечивается синим (`bg-indigo-50`) или серым (`bg-gray-200` для отключенных) фоном.
    -   **Счетчик постов**: Имеет цвет, зависящий от количества постов (красный для 0, оранжевый для <5, и т.д.).
    -   **Состояние ошибки (⚠️)**: Появляется иконка предупреждения при проблемах с доступом.
    -   **Индикатор обновления (🔵)**: Пульсирующая точка сигнализирует о наличии новых данных на сервере.
    -   **Индикатор загрузки (спиннер)**: Появляется вместо счетчика во время ручного или массового обновления.

### 3. Заголовок Расписания (ScheduleHeader)

Этот заголовок находится над сеткой календаря и содержит элементы управления для текущего вида.

-   **Структура и Адаптивность**:
    -   Компонент построен на `flexbox` с `flex-wrap` для обеспечения полной адаптивности.
    -   Все элементы управления разделены на две основные логические группы:
        1.  **Навигация (слева)**: Название месяца, переключатель "Неделя"/"Сегодня" и кнопки навигации по неделям.
        2.  **Действия (справа)**: Кнопки управления видимостью (заметки, теги), кнопка "Обновить" с выпадающим меню и блок "Выбрать" для массовых операций.
    -   **Поведение при сужении экрана**:
        -   При уменьшении ширины окна элементы внутри группы "Действия" начинают плавно переноситься на новую строку.
        -   При сильном сужении вся группа "Действия" целиком перемещается под группу "Навигация".
        -   Это гарантирует, что все инструменты всегда остаются видимыми и доступными.
-   **Динамические элементы**:
    -   При активации режима выбора появляются кнопки для управления выделенными элементами (снять выделение, удалить).
    -   **Гранулярное обновление**: Кнопка "Обновить" содержит выпадающее меню, которое позволяет пользователю точечно обновлять разные типы данных (опубликованные, отложенные, системные посты, теги, заметки), каждая со своим индикатором загрузки.

---

### 4. Стандартный макет заголовка вкладки (Tab Header Pattern)

Для обеспечения единообразия во всем приложении, все основные вкладки (такие как "Расписание" и "Предложенные посты") должны использовать стандартизированный двух-уровневый макет заголовка.

-   **Цель**: Отделить статический заголовок от интерактивных элементов управления, создавая четкую визуальную иерархию и предсказуемость интерфейса.

-   **Структура**:
    1.  **Верхний уровень (Заголовок)**:
        -   Содержит только название вкладки (`<h1>`) и, возможно, краткое описание (`<p>`).
        -   **Классы**: `p-4 border-b border-gray-200`.

    2.  **Нижний уровень (Панель управления)**:
        -   Содержит **все** интерактивные элементы, относящиеся к данной вкладке: кнопки ("Обновить"), навигацию, фильтры и т.д.
        -   **Классы**: `p-4 border-b border-gray-200`.

-   **Реализация**: Этот паттерн реализован в компонентах `ScheduleHeader.tsx`, `SuggestedPostsTab.tsx` и `DatabaseManagementPage.tsx` и является эталоном для будущих разделов.
