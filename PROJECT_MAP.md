
# Карта Проекта (Project Map)

Этот документ служит навигационной картой для разработчика и AI. Он описывает структуру, ключевые узлы и потоки данных приложения `VK Content Helper`.

---

## 1. Общая Архитектура

- **Тип:** Client-Server SPA.
- **Frontend:** React + TypeScript + Tailwind CSS (Vite).
- **Backend:** Python + FastAPI + SQLAlchemy + SQLite/PostgreSQL.
- **Взаимодействие:** REST API (JSON payloads).
- **Деплой:** Yandex Cloud (S3 для фронта, Serverless Containers для бека).

---

## 2. Frontend (`src/`)

Приложение построено по принципу **Feature-Sliced Design**.
**➡️ Подробная документация по фронтенду находится в файле `frontend_docs/DOCUMENTATION_MAP.md`**

### 🗂 Глобальное состояние (`src/contexts/`)
*   **`ProjectsContext`**: "Божественный объект" данных. Хранит проекты, посты всех типов, заметки, переменные.
    *   *Реализация:* Использует композицию хуков (`useDataInitialization`, `useUpdatePolling`, `useDataRefreshers`).
*   **`AuthContext`**: Управление сессией пользователя (`user`, `login`, `logout`).

### 🧩 Ключевые Фичи (`src/features/`)

#### 👥 `lists/` (Системные списки)
*   **`SystemListsTab.tsx`**: Точка входа (Вкладка "Списки").
*   **`hooks/useSystemListsManager.ts`**: Фасад для логики списков. Объединяет:
    *   `useListState`: Локальный стейт UI и данных.
    *   `useListFetching`: Обычная загрузка (GET).
    *   `useListPolling`: Управление фоновыми задачами (Task Monitor).
*   **`components/`**:
    *   `ListCard.tsx`: Навигационная карточка со статистикой.
    *   `MembersTable.tsx`, `PostsTable.tsx`, `InteractionTable.tsx`: Таблицы данных.
    *   `ListStatisticsPanel.tsx`: Графики и сводка.

#### 📦 `products/` (Товары)
Самый сложный модуль на данный момент.
*   **`ProductsTab.tsx`**: Точка входа.
*   **`hooks/useProductsManager.ts`**: "Мозг" модуля. Композитный хук, объединяющий 8 суб-хуков (`useProductData`, `useProductEditing` и т.д.).
*   **`components/table/`**: Реализация редактируемой таблицы (`ProductRow`, `ActionCell`, `DescriptionCell` с авто-высотой).
*   **`components/modals/`**: Массовое редактирование, создание товаров, AI-подсказки.

#### 🗓 `schedule/` (Календарь)
*   **`ScheduleTab.tsx`**: Контейнер календаря.
*   **`hooks/useScheduleManager.ts`**: Композитный хук. Управляет данными, Drag-and-Drop (`useScheduleInteraction`) и модальными окнами (`useScheduleModals`).
*   **`components/ScheduleGrid.tsx`**: Сетка дней. Рендерит `PostCard` и `NoteCard`.

#### 📝 `posts/` (Посты)
*   **`components/PostCard.tsx`**: Карточка поста. Использует `usePostCardActions` для логики кнопок. Мемоизирован для производительности.
*   **`components/modals/PostDetailsModal.tsx`**: Окно создания/редактирования.
    *   Декомпозирован на: `PostTextSection`, `PostMediaSection` (галерея/загрузка), `PostDateTimePicker`.
    *   Использует `hooks/usePostDetails.ts` и `usePostForm.ts`.
*   **`components/AIGenerator.tsx`**: Чат с AI. Использует `hooks/useAIGenerator.ts`.

#### ⚙️ `projects/` (Проекты)
*   **`Sidebar.tsx`**: Левая панель навигации и фильтрации.
*   **`components/modals/ProjectSettingsModal.tsx`**: Настройки проекта. Содержит редакторы:
    *   `VariablesEditor`: Локальные переменные.
    *   `GlobalVariablesEditor`: Глобальные переменные (значения + определения).
    *   `TagsEditor`: Теги.
    *   `AiPromptPresetsEditor`: Шаблоны AI.

### 🔌 Слой API (`src/services/`)
*   **`api.ts`**: **Хаб**. Просто ре-экспортирует всё из папки `api/`.
*   **`api/*.api.ts`**: Модульные файлы (`post.api.ts`, `market.api.ts`, `lists.api.ts` и т.д.).
*   **`shared/utils/apiClient.ts`**: Функция `callApi`. Обрабатывает `fetch`, ошибки и retry-логику.

---

## 3. Backend (`backend_python/`)

Модульная структура на FastAPI.

**➡️ Подробная документация по бэкенду находится в файле `backend_python/docs/DOCUMENTATION_MAP.md.txt`**

### 🧱 Ядро
*   **`main.py`**: Инициализация App, подключение роутеров, CORS.
*   **`database.py`**: Подключение к БД (SQLite локально, Postgres в облаке).
*   **`models.py`**: SQLAlchemy модели (Таблицы: `Project`, `Post`, `ScheduledPost`, `SystemPost`, `MarketItem`, `SystemListSubscriber`...).
*   **`schemas/`**: Pydantic схемы (Валидация входа/выхода API).

### 🧠 Бизнес-логика (`services/`)
*   **`lists/`**: Пакет сервисов для списков.
    *   `system_list_service.py` (Фасад), `list_retrieval.py`, `list_sync_*.py`.
*   **`task_monitor.py`**: Менеджер фоновых задач для длительных операций.
*   **`vk_service.py`**: Высокоуровневый фасад для VK.
*   **`vk_api/`**: Низкоуровневый клиент (`api_client.py` с ретраями) и форматтеры.
*   **`post_tracker_service.py`**: **Фоновый процесс**. Публикует системные посты и проверяет их выход на стене.
*   **`market_service.py`**: Логика товаров (синхронизация, создание, обновление).
*   **`market_ai_service.py`**: Логика AI для товаров (подбор категорий, коррекция текста).
*   **`gemini_service.py`**: Клиент к Google Gemini (через прокси).

### 🛣 Роутеры (`routers/`)
Контроллеры, принимающие HTTP запросы. Просто вызывают сервисы.
*   `lists.py`: Эндпоинты для списков и фоновых задач.

### 💾 Доступ к данным (`crud/`)
Прямые SQL-запросы через SQLAlchemy. Никакой бизнес-логики, только чтение/запись.
*   `lists/`: Пакет CRUD для списков (`subscribers.py`, `posts.py` и т.д.).

---

## 4. Ключевые Механики

### Системные Списки (Синхронизация)
1.  **Запуск:** Фронтенд вызывает эндпоинт запуска задачи (например, `refreshSubscribers`).
2.  **Task Monitor:** Бэкенд создает `taskId` и запускает задачу в фоне (`BackgroundTasks`).
3.  **Polling:** Фронтенд периодически опрашивает статус (`getTaskStatus`).
4.  **Процесс:** Задача итеративно скачивает данные из VK, сравнивает с БД (diff) и обновляет записи.
5.  **Финал:** Задача обновляет метаданные проекта (счетчики, время).

### Системные Посты
1.  Создаются со статусом `pending_publication` в таблице `system_posts`.
2.  `post_tracker_service` (в фоне) находит их, когда наступает время.
3.  Меняет статус на `publishing`, отправляет в VK (`wall.post`).
4.  Записывает `vk_post_id`.
5.  Через время проверяет наличие поста на стене (`verification`).
6.  Если найден -> удаляет из `system_posts` (пост становится обычным "Опубликованным").

### Глобальные Переменные
*   **Определения**: Хранятся в `global_variable_definitions`. Это "ключи" (например, `{global_phone}`).
*   **Значения**: Хранятся в `project_global_variable_values`. Привязаны к проекту.
*   **Подстановка**: Происходит на бэкенде в момент публикации (`services/global_variable_service.py` -> `substitute_global_variables`).

### Товары (Market)
*   **Кеширование**: Данные (товары, альбомы, категории) жестко кешируются в БД.
*   **Обновление**: При входе на вкладку проверяется дата кеша. Если старый -> полная перезагрузка из VK.
*   **Инлайн-редактирование**: Изменения копятся на фронте, при сохранении отправляются на бэк. Бэк обновляет VK (`market.edit`), обновляет кеш и возвращает актуальные данные.

### AI (Gemini)
*   Используется для: генерации постов, коррекции отзывов, подбора категорий товаров, заполнения переменных.
*   **Прокси**: Работает через SOCKS5 прокси (настройка в `.env`), чтобы обойти геоблок.

---

## 5. Правила Разработки

1.  **Модификация API**:
    *   Сначала обновить Pydantic-схемы (`backend_python/schemas/`).
    *   Реализовать логику в `services/`.
    *   Создать/обновить эндпоинт в `routers/`.
    *   Добавить функцию в `src/services/api/`.
2.  **Frontend State**: Не использовать `useEffect` для данных внутри компонентов. Использовать `useProjects` или специализированные хуки-менеджеры.
3.  **Стабильность**: Компоненты внутри `render` (например, внутри `map`) должны быть вынесены в отдельные файлы, чтобы избежать пересоздания при ре-рендере (проблема "моргания").